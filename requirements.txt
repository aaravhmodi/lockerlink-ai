# PyTorch for SAM3 model inference
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# FastAPI web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9

# Image and video processing
opencv-python==4.10.0.84
numpy==2.1.1
pillow==11.0.0

# HTTP requests for video downloads
requests==2.32.3

# HuggingFace Transformers for SAM3
transformers>=4.45.0
accelerate>=0.30.0
huggingface_hub>=0.20.0

# Environment variable management
python-dotenv>=1.0.0

# Note: SAM3 uses HuggingFace Transformers library (local model)
# Authentication required: Set HF_TOKEN in .env.local file (not committed to git)
# Model will be downloaded from HuggingFace on first use

